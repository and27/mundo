# mundo (monorepo)

## Structure
- `apps/web` – Next.js app
- `packages/core` – shared domain core (planned)

## Workspace scripts (pnpm)
```bash
pnpm dev:web
pnpm build:web
pnpm start:web
pnpm lint:web
```
